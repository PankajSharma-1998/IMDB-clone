import React from 'react';
import Header from '../Header/Header';
import Content from '../Content/Content';

const Home = (props) => {
    return (
        <div className="home">  
      <Header />
      <Content />
        </div>
    );
};

export default Home;